 <hc:Window x:Class="OsuLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OsuLauncher"
        mc:Ignorable="d"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        WindowStartupLocation="CenterScreen"
        Height="600" Width="1066">
    <hc:Window.NonClientAreaContent>
        <DockPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                <Button Name="BeatmapNavButton" Content="Content" hc:BorderElement.CornerRadius="1" Height="35" Width="80" Style="{StaticResource ButtonCustom}"/>
                <!-- <Button Name="CollectionsNavButton" Content="Collections" hc:BorderElement.CornerRadius="1" Height="35" Width="80" Style="{StaticResource ButtonCustom}"/> -->
                <!-- ReSharper disable once Xaml.StaticResourceNotResolved -->
                <Button Name="NewsNavButton" Content="News" hc:BorderElement.CornerRadius="1" Height="35" Width="80" Style="{StaticResource ButtonCustom}"/>
                <!-- <Button Name="WikiNavButton" Content="Wiki" hc:BorderElement.CornerRadius="1" Height="35" Width="80" Style="{StaticResource ButtonCustom}"/> -->
                <!-- ReSharper disable once IdentifierTypo -->
                <!--<Button Name="ReplaysNavButton" Content="Replays" hc:BorderElement.CornerRadius="1" Height="35" Width="80" Style="{StaticResource ButtonCustom}"/>-->
                <Button Name="AccountNavButton" Content="My Account" hc:BorderElement.CornerRadius="1" Height="35" Width="80" Style="{StaticResource ButtonCustom}"/>
                <Button Name="SettingsNavButton" Content="Settings" HorizontalAlignment="Right" hc:BorderElement.CornerRadius="1" Height="35" Width="80" Style="{StaticResource ButtonCustom}"/>
            </StackPanel>
        </DockPanel>
    </hc:Window.NonClientAreaContent>
     
    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom">
            <StackPanel Orientation="Horizontal">
                <Button Name="PlayButton" Content="Play" Foreground="White" Background="#eb69a3" HorizontalAlignment="Left" Height="35" Width="100" hc:BorderElement.CornerRadius="0" Margin="4,0,1,0"/>
            </StackPanel>
            <Border BorderThickness="2" BorderBrush="Black">
                <StackPanel Orientation="Horizontal" Margin="3">
                    <hc:ImageBlock Name="AvatarBlock" Width="35" Height="35"/>
                    <StackPanel>
                        <TextBlock Name="UsernameText" FontSize="10"/>
                        <TextBlock Name="AccuracyText" FontSize="10"/>
                        <!-- ReSharper disable once InconsistentNaming -->
                        <TextBlock Name="PPRankText" FontSize="10"/>
                        <TextBlock Name="LevelText" FontSize="10"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
        <Frame Name="MainFrame" NavigationUIVisibility="Hidden"/>
    </DockPanel>
</hc:Window>
